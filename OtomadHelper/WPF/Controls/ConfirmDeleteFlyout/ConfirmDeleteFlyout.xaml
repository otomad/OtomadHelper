<c:BaseFlyout
	x:Class="OtomadHelper.WPF.Controls.ConfirmDeleteFlyout"
	Title="ConfirmDeleteFlyout"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:s="clr-namespace:System;assembly=mscorlib"
	Width="400"
	Height="200"
	SizeToContent="Height"
	Topmost="True"
	Showing="ConfirmDeleteFlyout_Showing"
	mc:Ignorable="d"
	>
	<c:BaseFlyout.DataContext>
		<c:ConfirmDeleteFlyoutViewModel />
	</c:BaseFlyout.DataContext>

	<c:BaseFlyout.Resources>
		<Style x:Key="ConfirmMessage" TargetType="TextBlock">
			<!-- Must specify a x:Key, otherwise even the AccessKey inside the Button will still be unexpectedly affected by this TextBlock style. -->
			<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="Margin" Value="0,0,0,12" />
		</Style>

		<Style
			x:Key="Title"
			BasedOn="{StaticResource ConfirmMessage}"
			TargetType="TextBlock"
			>
			<Setter Property="FontWeight" Value="SemiBold" />
			<Setter Property="Margin" Value="0,0,0,5" />
		</Style>

		<Style
			x:Key="SmallShadingIcon"
			BasedOn="{StaticResource ShadingIcon}"
			TargetType="c:Icon"
			>
			<Setter Property="Size" Value="48" />
			<Setter Property="Canvas.Left" Value="-2.5" />
			<Setter Property="Canvas.Top" Value="-2.5" />
		</Style>
	</c:BaseFlyout.Resources>

	<Grid>
		<Canvas>
			<c:Icon IconName="Delete" Style="{StaticResource SmallShadingIcon}" />
		</Canvas>
		<Border Padding="16,14" BorderThickness="0">
			<StackPanel Orientation="Vertical">
				<TextBlock Style="{StaticResource Title}" Text="{m:Trans Flyout.ConfirmDelete.Title}" />
				<TextBlock Style="{StaticResource ConfirmMessage}" d:Text="Are you sure you want to delete it?" Text="{Binding Message}" />
				<UniformGrid>
					<c:Button
						Command="{Binding SetDialogResultCommand}"
						CommandParameter="{m:Bool True}"
						Content="{m:Trans ContentDialog.Button.Ok}"
						/>
					<c:Button
						Command="{Binding SetDialogResultCommand}"
						CommandParameter="{m:Bool False}"
						Content="{m:Trans ContentDialog.Button.Cancel}"
						IsDefault="True"
						/>
				</UniformGrid>
			</StackPanel>
		</Border>
	</Grid>
</c:BaseFlyout>
