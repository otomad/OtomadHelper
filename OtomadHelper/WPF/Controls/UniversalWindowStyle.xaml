<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:s="clr-namespace:System;assembly=mscorlib"
	>
	<!--
		This add a custom title bar buttons for some missing features in Windows 10 ~ Windows 11 21H2 only.
	-->
	<Style x:Key="UniversalWindowStyle" TargetType="c:BackdropWindow">
		<Style.Triggers>
			<Trigger Property="UseUniversalControlBox" Value="True">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="c:BackdropWindow">
							<Border Margin="0">
								<Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
									<Grid
										x:Name="TitleBarPanel"
										Height="31"
										VerticalAlignment="Top"
										>
										<FrameworkElement.Resources>
											<Style x:Key="ButtonIcon" TargetType="TextBlock">
												<Setter Property="HorizontalAlignment" Value="Center" />
												<Setter Property="VerticalAlignment" Value="Center" />
												<Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
												<Setter Property="FontSize" Value="10" />
												<Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
												<Style.Triggers>
													<DataTrigger Binding="{Binding FlowDirection, RelativeSource={RelativeSource AncestorType=Window}}" Value="{x:Static FlowDirection.RightToLeft}">
														<Setter Property="RenderTransform">
															<Setter.Value>
																<ScaleTransform ScaleX="-1" />
															</Setter.Value>
														</Setter>
													</DataTrigger>
												</Style.Triggers>
											</Style>

											<Style TargetType="Button">
												<Setter Property="Width" Value="46" />
												<Setter Property="BorderThickness" Value="0" />
												<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
												<Setter Property="Background" Value="Transparent" />
												<Setter Property="Focusable" Value="False" />
												<Setter Property="AutomationProperties.Name" Value="{Binding ToolTip, RelativeSource={RelativeSource Self}}" />
												<Setter Property="AutomationProperties.HelpText" Value=" " />
												<Setter Property="Stylus.IsPressAndHoldEnabled" Value="False" />
												<Setter Property="Stylus.IsFlicksEnabled" Value="False" />
												<Setter Property="Stylus.IsTapFeedbackEnabled" Value="False" />
												<Setter Property="Stylus.IsTouchFeedbackEnabled" Value="False" />
												<Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
												<Setter Property="Template">
													<Setter.Value>
														<ControlTemplate TargetType="Button">
															<Border
																Name="OverBorder"
																Background="{TemplateBinding Background}"
																BorderThickness="0,1,0,0"
																>
																<TextBlock
																	x:Name="MinimizeIcon"
																	Foreground="{TemplateBinding Foreground}"
																	Style="{StaticResource ButtonIcon}"
																	Text="{TemplateBinding Content}"
																	/>
															</Border>
														</ControlTemplate>
													</Setter.Value>
												</Setter>
												<Style.Triggers>
													<DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
														<Setter Property="Foreground" Value="{DynamicResource TextTertiary}" />
													</DataTrigger>
													<MultiTrigger>
														<!--  When the pointer is over the button.  -->
														<MultiTrigger.Conditions>
															<Condition Property="IsMouseOver" Value="True" />
															<Condition Property="IsStylusOver" Value="False" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
														<Setter Property="Background" Value="{DynamicResource SubtleSecondary}" />
													</MultiTrigger>
													<!--  When the pointer is pressed.  -->
													<MultiTrigger>
														<MultiTrigger.Conditions>
															<Condition Property="IsPressed" Value="True" />
															<Condition Property="AreAnyTouchesOver" Value="False" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
														<Setter Property="Background" Value="{DynamicResource SubtleTertiary}" />
													</MultiTrigger>
													<!--  When the touch device is pressed.  -->
													<MultiTrigger>
														<MultiTrigger.Conditions>
															<Condition Property="IsPressed" Value="True" />
															<Condition Property="AreAnyTouchesOver" Value="True" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
														<Setter Property="Background" Value="{DynamicResource SubtleTertiary}" />
													</MultiTrigger>
													<MultiDataTrigger>
														<MultiDataTrigger.Conditions>
															<Condition Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="False" />
															<Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="False" />
														</MultiDataTrigger.Conditions>
														<Setter Property="Opacity" Value="0.6" />
													</MultiDataTrigger>
													<DataTrigger Binding="{Binding IsHighContrast, RelativeSource={RelativeSource AncestorType=c:BackdropWindow}}" Value="True">
														<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
													</DataTrigger>
													<MultiDataTrigger>
														<MultiDataTrigger.Conditions>
															<Condition Binding="{Binding IsHighContrast, RelativeSource={RelativeSource AncestorType=c:BackdropWindow}}" Value="True" />
															<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
														</MultiDataTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
													</MultiDataTrigger>
													<Trigger Property="IsEnabled" Value="False">
														<Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
													</Trigger>
												</Style.Triggers>
											</Style>
											<Style
												x:Key="Style.Button.Close"
												BasedOn="{StaticResource {x:Type Button}}"
												TargetType="Button"
												>
												<Style.Triggers>
													<MultiTrigger>
														<!--  When the pointer is over the button.  -->
														<MultiTrigger.Conditions>
															<Condition Property="IsMouseOver" Value="True" />
															<Condition Property="IsStylusOver" Value="False" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource CaptionCloseText}" />
														<Setter Property="Background" Value="{DynamicResource CaptionCloseFill}" />
													</MultiTrigger>
													<!--  When the pointer is pressed.  -->
													<MultiTrigger>
														<MultiTrigger.Conditions>
															<Condition Property="IsPressed" Value="True" />
															<Condition Property="AreAnyTouchesOver" Value="False" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource CaptionCloseTextSecondary}" />
														<Setter Property="Background" Value="{DynamicResource CaptionCloseFillSecondary}" />
													</MultiTrigger>
													<!--  When the touch device is pressed.  -->
													<MultiTrigger>
														<MultiTrigger.Conditions>
															<Condition Property="IsPressed" Value="True" />
															<Condition Property="AreAnyTouchesOver" Value="True" />
														</MultiTrigger.Conditions>
														<Setter Property="Foreground" Value="{DynamicResource CaptionCloseTextSecondary}" />
														<Setter Property="Background" Value="{DynamicResource CaptionCloseFillSecondary}" />
													</MultiTrigger>
												</Style.Triggers>
											</Style>
										</FrameworkElement.Resources>
										<StackPanel
											x:Name="TitleBarButtonPanel"
											Margin="0,-1,0,0"
											HorizontalAlignment="Right"
											Orientation="Horizontal"
											>
											<Button
												x:Name="MinimizeButton"
												Content="&#xE921;"
												ToolTip="{m:Trans Window.Minimize}"
												>
												<i:Interaction.Triggers>
													<i:EventTrigger EventName="Click">
														<i:ChangePropertyAction
															PropertyName="WindowState"
															TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
															Value="{x:Static WindowState.Minimized}"
															/>
													</i:EventTrigger>
												</i:Interaction.Triggers>
											</Button>
											<Button
												x:Name="RestoreButton"
												Content="&#xE923;"
												ToolTip="{m:Trans Window.Restore}"
												>
												<i:Interaction.Triggers>
													<i:EventTrigger EventName="Click">
														<i:ChangePropertyAction
															PropertyName="WindowState"
															TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
															Value="{x:Static WindowState.Normal}"
															/>
													</i:EventTrigger>
												</i:Interaction.Triggers>
												<i:Interaction.Behaviors>
													<m:SnapLayoutButtonBehavior />
												</i:Interaction.Behaviors>
											</Button>
											<Button
												x:Name="MaximizeButton"
												Content="&#xE922;"
												ToolTip="{m:Trans Window.Maximize}"
												>
												<i:Interaction.Triggers>
													<i:EventTrigger EventName="Click">
														<i:ChangePropertyAction
															PropertyName="WindowState"
															TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
															Value="{x:Static WindowState.Maximized}"
															/>
													</i:EventTrigger>
												</i:Interaction.Triggers>
												<i:Interaction.Behaviors>
													<m:SnapLayoutButtonBehavior />
												</i:Interaction.Behaviors>
											</Button>
											<Button
												x:Name="CloseButton"
												Content="&#xE8BB;"
												Style="{DynamicResource Style.Button.Close}"
												ToolTip="{m:Trans Window.Close}"
												>
												<i:Interaction.Triggers>
													<i:EventTrigger EventName="Click">
														<i:CallMethodAction MethodName="Close" TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
													</i:EventTrigger>
												</i:Interaction.Triggers>
											</Button>
										</StackPanel>
									</Grid>
									<AdornerDecorator>
										<ContentPresenter />
									</AdornerDecorator>
								</Grid>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="WindowState" Value="Maximized">
									<Setter TargetName="RootGrid" Property="Margin" Value="4,4,7,4" />
									<Setter TargetName="TitleBarPanel" Property="Height" Value="30" />
									<Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed" />
								</Trigger>
								<Trigger Property="WindowState" Value="Normal">
									<Setter TargetName="RestoreButton" Property="Visibility" Value="Collapsed" />
								</Trigger>
								<Trigger Property="WindowState" Value="Minimized">
									<Setter TargetName="MinimizeButton" Property="Visibility" Value="Collapsed" />
								</Trigger>
								<Trigger Property="ControlBox" Value="False">
									<Setter Property="Visibility" Value="Hidden" />
								</Trigger>
								<Trigger Property="MinimizeBox" Value="False">
									<Setter TargetName="MinimizeButton" Property="IsEnabled" Value="False" />
								</Trigger>
								<Trigger Property="MaximizeBox" Value="False">
									<Setter TargetName="MaximizeButton" Property="IsEnabled" Value="False" />
								</Trigger>
								<MultiTrigger>
									<MultiTrigger.Conditions>
										<Condition Property="MinimizeBox" Value="False" />
										<Condition Property="MaximizeBox" Value="False" />
									</MultiTrigger.Conditions>
									<Setter TargetName="MinimizeButton" Property="Visibility" Value="Collapsed" />
									<Setter TargetName="RestoreButton" Property="Visibility" Value="Collapsed" />
									<Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed" />
								</MultiTrigger>
								<Trigger Property="ResizeMode" Value="NoResize">
									<Setter TargetName="MinimizeButton" Property="Visibility" Value="Collapsed" />
									<Setter TargetName="RestoreButton" Property="Visibility" Value="Collapsed" />
									<Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed" />
								</Trigger>
								<Trigger Property="ResizeMode" Value="CanMinimize">
									<Setter TargetName="MaximizeButton" Property="IsEnabled" Value="False" />
								</Trigger>
								<Trigger Property="IsHighContrast" Value="True">
									<Setter TargetName="MinimizeButton" Property="Content" Value="&#xEF2D;" />
									<Setter TargetName="RestoreButton" Property="Content" Value="&#xEF2F;" />
									<Setter TargetName="MaximizeButton" Property="Content" Value="&#xEF2E;" />
									<Setter TargetName="CloseButton" Property="Content" Value="&#xEF2C;" />
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Trigger>
		</Style.Triggers>
	</Style>
</ResourceDictionary>
