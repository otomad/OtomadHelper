<m:CustomControlResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:s="clr-namespace:System;assembly=mscorlib"
	>
	<Style x:Key="DefaultToggleButtonStyle" TargetType="ToggleButton">
		<Setter Property="OverridesDefaultStyle" Value="true" />
		<Setter Property="Focusable" Value="False" />
		<Setter Property="AutomationProperties.Name" Value="{Binding Content}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ToggleButton">
					<ControlTemplate.Resources>
						<m:BooleanInversionConverter x:Key="BoolInvert" />
					</ControlTemplate.Resources>
					<c:Button
						Width="{TemplateBinding Width}"
						Height="{TemplateBinding Height}"
						MinHeight="{TemplateBinding MinHeight}"
						Padding="{TemplateBinding Padding}"
						IsDefault="{TemplateBinding IsChecked}"
						IsEnabled="{TemplateBinding IsEnabled}"
						Content="{TemplateBinding Content}"
						ClipToBounds="False"
						Command="{TemplateBinding Command}"
						CommandParameter="{TemplateBinding CommandParameter}"
						>
						<i:Interaction.Triggers>
							<i:EventTrigger EventName="Click">
								<i:ChangePropertyAction
									PropertyName="IsChecked"
									TargetObject="{Binding RelativeSource={RelativeSource TemplatedParent}}"
									Value="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolInvert}}"
									/>
							</i:EventTrigger>
						</i:Interaction.Triggers>
					</c:Button>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style BasedOn="{StaticResource DefaultToggleButtonStyle}" TargetType="ToggleButton" />
</m:CustomControlResourceDictionary>
