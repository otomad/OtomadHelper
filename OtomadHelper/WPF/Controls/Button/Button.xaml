<m:CustomControlResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:s="clr-namespace:System;assembly=mscorlib"
	>
	<Style x:Key="DefaultButtonStyle" TargetType="c:Button">
		<Style.Resources>
			<m:BooleanTruthyConverter x:Key="BoolTruthy" />
			<m:BackgroundToForegroundColorConverter x:Key="BgToFg" />
			<m:ObjectToTypeNameConverter x:Key="TypeOf" />
			<m:BooleanToVisibilityConverter x:Key="BoolToVis" />
			<m:BooleanAndConverter x:Key="BoolAnd" />
			<c:IsNotSolidColorToBooleanConverter x:Key="NotSolidColor" />
			<m:AccessKeyAmpersandToNothingConverter x:Key="RemoveAmp" />

			<CornerRadius x:Key="ButtonCornerRadius">4</CornerRadius>

			<Style x:Key="ButtonContentPresenter" TargetType="ContentPresenter">
				<Setter Property="HorizontalAlignment" Value="Center" />
				<Setter Property="VerticalAlignment" Value="Center" />
				<Setter Property="RecognizesAccessKey" Value="True" />
			</Style>

			<Style x:Key="FixExternalTextBlockStyleAffectingAccessKeyStyle" TargetType="TextBlock">
				<!--
					If there is any style for TextBlock outside the Button, it will unexpectedly affect the style of AccessKey inside the Button,
					and it needs to be cleaned up here.
				-->
				<Setter Property="Margin" Value="0" />
			</Style>

			<Style x:Key="ButtonIconTextContainer" TargetType="StackPanel">
				<Setter Property="Orientation" Value="Horizontal" />
				<Style.Resources>
					<Style TargetType="c:Icon">
						<Setter Property="Margin" Value="0,0,8,0" />
						<Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding IconName, RelativeSource={RelativeSource AncestorType=c:Button}}" Value="None">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
							<DataTrigger Binding="{Binding ShowIconOnly, RelativeSource={RelativeSource AncestorType=c:Button}}" Value="True">
								<Setter Property="Margin" Value="0" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
					<Style TargetType="c:ButtonContentTemplate">
						<Style.Triggers>
							<DataTrigger Binding="{Binding ShowIconOnly, RelativeSource={RelativeSource AncestorType=c:Button}}" Value="True">
								<Setter Property="Visibility" Value="Collapsed" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Style.Resources>
			</Style>
		</Style.Resources>

		<Setter Property="MinHeight" Value="32" />
		<Setter Property="BorderBrush" Value="{DynamicResource ControlElevation}" />
		<Setter Property="Background" Value="{DynamicResource ControlBackground}" />
		<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
		<Setter Property="BackgroundOpacity" Value="1" />
		<Setter Property="FontFamily" Value="{Binding DefaultFont, RelativeSource={RelativeSource AncestorType=c:BackdropWindow}}" />
		<Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
		<Setter Property="FocusVisualStyle" Value="{DynamicResource OuterFocusVisualStyle}" />
		<Setter Property="Padding" Value="8" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="AutomationProperties.Name" Value="{Binding Content, RelativeSource={RelativeSource Self}, Converter={StaticResource RemoveAmp}}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:Button">
					<Border
						Padding="{TemplateBinding Padding}"
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						CornerRadius="{StaticResource ButtonCornerRadius}"
						>
						<ContentPresenter Style="{StaticResource ButtonContentPresenter}">
							<ContentPresenter.ContentTemplate>
								<DataTemplate>
									<DataTemplate.Resources>
										<Style TargetType="TextBlock" BasedOn="{StaticResource FixExternalTextBlockStyleAffectingAccessKeyStyle}" />
									</DataTemplate.Resources>
									<StackPanel Style="{StaticResource ButtonIconTextContainer}">
										<c:Icon IconName="{Binding IconName, RelativeSource={RelativeSource AncestorType=c:Button}}" />
										<c:ButtonContentTemplate Text="{Binding}" />
									</StackPanel>
								</DataTemplate>
							</ContentPresenter.ContentTemplate>
						</ContentPresenter>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="Background" Value="{DynamicResource ControlBackgroundSecondary}" />
				<Setter Property="BackgroundOpacity" Value="{DynamicResource AccentSecondaryOpacity}" />
			</Trigger>
			<Trigger Property="IsPressed" Value="True">
				<Setter Property="BorderBrush" Value="{DynamicResource ControlStroke}" />
				<Setter Property="BackgroundOpacity" Value="{DynamicResource AccentTertiaryOpacity}" />
				<Setter Property="Background" Value="{DynamicResource ControlBackgroundTertiary}" />
				<Setter Property="Foreground">
					<Setter.Value>
						<SolidColorBrush Opacity="{DynamicResource ControlPressedOpacity}" Color="{DynamicResource ForegroundColor}" />
					</Setter.Value>
				</Setter>
			</Trigger>
			<Trigger Property="IsDefault" Value="True">
				<Setter Property="Accent" Value="{Binding WindowGlassColor, RelativeSource={RelativeSource AncestorType=c:BackdropWindow}}" />
			</Trigger>
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
				<Setter Property="Background" Value="{DynamicResource ControlDisabled}" />
				<Setter Property="BorderBrush" Value="{DynamicResource ControlStroke}" />
			</Trigger>
			<DataTrigger Binding="{Binding Accent, RelativeSource={RelativeSource Self}, Converter={StaticResource BoolTruthy}}" Value="True">
				<Setter Property="BorderBrush" Value="{DynamicResource AccentControlElevation}" />
				<Setter Property="Background">
					<Setter.Value>
						<SolidColorBrush Opacity="{Binding BackgroundOpacity, RelativeSource={RelativeSource AncestorType=c:Button}}" Color="{Binding Accent, RelativeSource={RelativeSource AncestorType=c:Button}}" />
					</Setter.Value>
				</Setter>
				<Setter Property="Foreground" Value="{Binding Background, RelativeSource={RelativeSource Self}, Converter={StaticResource BgToFg}}" />
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="c:Button">
							<Grid>
								<Border
									Padding="{TemplateBinding Padding}"
									Background="{DynamicResource TranslucentCheckerBoard}"
									CornerRadius="{StaticResource ButtonCornerRadius}"
									Opacity="{TemplateBinding BackgroundOpacity}"
									>
									<Border.Visibility>
										<MultiBinding Converter="{StaticResource BoolAnd}">
											<Binding Path="MixCheckerBoard" RelativeSource="{RelativeSource TemplatedParent}" />
											<Binding Path="Background" RelativeSource="{RelativeSource TemplatedParent}" Converter="{StaticResource NotSolidColor}" />
										</MultiBinding>
									</Border.Visibility>
								</Border>
								<Border
									Padding="{TemplateBinding Padding}"
									Background="{TemplateBinding Background}"
									CornerRadius="{StaticResource ButtonCornerRadius}"
									>
									<ContentPresenter Style="{StaticResource ButtonContentPresenter}">
										<ContentPresenter.ContentTemplate>
											<DataTemplate>
												<DataTemplate.Resources>
													<Style TargetType="TextBlock" BasedOn="{StaticResource FixExternalTextBlockStyleAffectingAccessKeyStyle}" />
												</DataTemplate.Resources>
												<StackPanel Style="{StaticResource ButtonIconTextContainer}">
													<c:Icon IconName="{Binding IconName, RelativeSource={RelativeSource AncestorType=c:Button}}" />
													<c:ButtonContentTemplate Text="{Binding}" />
												</StackPanel>
											</DataTemplate>
										</ContentPresenter.ContentTemplate>
									</ContentPresenter>
								</Border>
								<Border
									Padding="{TemplateBinding Padding}"
									BorderBrush="{TemplateBinding BorderBrush}"
									BorderThickness="{TemplateBinding BorderThickness}"
									CornerRadius="{StaticResource ButtonCornerRadius}"
									/>
							</Grid>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</DataTrigger>
			<MultiDataTrigger>
				<MultiDataTrigger.Conditions>
					<Condition Binding="{Binding Accent, RelativeSource={RelativeSource Self}, Converter={StaticResource BoolTruthy}}" Value="True" />
					<Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="True" />
				</MultiDataTrigger.Conditions>
				<Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeOnAccent}" />
				<Setter Property="Foreground">
					<Setter.Value>
						<SolidColorBrush Opacity="{DynamicResource ControlPressedOpacityOnAccent}" Color="{Binding Background, RelativeSource={RelativeSource AncestorType=c:Button}, Converter={StaticResource BgToFg}}" />
					</Setter.Value>
				</Setter>
			</MultiDataTrigger>
			<MultiTrigger>
				<MultiTrigger.Conditions>
					<Condition Property="IsEnabled" Value="False" />
					<Condition Property="IsDefault" Value="True" />
				</MultiTrigger.Conditions>
				<Setter Property="Foreground" Value="{DynamicResource TextOnAccentDisabled}" />
				<Setter Property="Background" Value="{DynamicResource AccentDisabled}" />
				<Setter Property="BorderBrush" Value="{DynamicResource AccentDisabled}" />
				<Setter Property="BorderThickness" Value="0" />
			</MultiTrigger>
			<Trigger Property="ShowIconOnly" Value="True">
				<Setter Property="ToolTip">
					<Setter.Value>
						<ToolTip DataContext="{Binding Path=PlacementTarget, RelativeSource={RelativeSource Self}}">
							<StackPanel>
								<c:ButtonContentTemplate Text="{Binding Content, FallbackValue=''}" FontWeight="Bold" Margin="0,0,0,2" />
								<ContentControl Content="{Binding (AutomationProperties.HelpText), FallbackValue=''}" />
							</StackPanel>
						</ToolTip>
					</Setter.Value>
				</Setter>
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="c:Button" />
	<!--<Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="Button" />-->
</m:CustomControlResourceDictionary>
