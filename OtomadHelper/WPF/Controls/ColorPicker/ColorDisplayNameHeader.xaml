<UserControl
	x:Class="OtomadHelper.WPF.Controls.ColorDisplayNameHeader"
	x:Name="View"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="450"
	d:DesignWidth="800"
	mc:Ignorable="d"
	Margin="0,0,2,0"
	>
	<UserControl.Resources>
		<Style TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Right" />
			<Setter Property="FontSize" Value="{DynamicResource CaptionFontSize}" />
			<Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
		</Style>
		<Style x:Key="ModelAxisTextBlock" BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
			<Setter Property="FontStyle" Value="Italic" />
		</Style>
		<Style x:Key="ColorTextBlock" BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
			<Setter Property="FontWeight" Value="Bold" />
			<Style.Triggers>
				<DataTrigger Binding="{Binding Color}" Value="">
					<Setter Property="Visibility" Value="Collapsed" />
				</DataTrigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
	<StackPanel DataContext="{Binding ElementName=View}">
		<TextBlock Style="{StaticResource ModelAxisTextBlock}" Text="{Binding ModelAxis}" />
		<TextBlock Style="{StaticResource ColorTextBlock}" Text="{Binding Color}" />
	</StackPanel>
</UserControl>
