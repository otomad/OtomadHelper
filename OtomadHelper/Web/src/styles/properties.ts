/**
 * 聚焦环外缘盒子阴影长度运动曲线单独控制。
 */
CSS.registerProperty({
	name: "--focus-ring-length-outer",
	syntax: "<length>",
	inherits: false,
	initialValue: "0",
});

/**
 * 聚焦环外缘盒子阴影长度运动曲线单独控制。
 */
CSS.registerProperty({
	name: "--focus-ring-length-inner",
	syntax: "<length>",
	inherits: false,
	initialValue: "0",
});
