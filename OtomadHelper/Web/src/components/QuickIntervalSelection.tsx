const StyledPreviewQuickIntervalSelection = styled.div`

`;

function PreviewQuickIntervalSelection({ interval }: {
	interval: number;
}) {
	return (
		<StyledPreviewQuickIntervalSelection>

		</StyledPreviewQuickIntervalSelection>
	);
}

export default function QuickIntervalSelection({ }: {

}) {
	return (
		<PreviewQuickIntervalSelection />
	);
}
