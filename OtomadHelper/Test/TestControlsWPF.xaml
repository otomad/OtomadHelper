<c:BackdropWindow
	x:Class="OtomadHelper.Test.TestControlsWPF"
	Title="TestControlsWPF"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:c="clr-namespace:OtomadHelper.WPF.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:m="clr-namespace:OtomadHelper.WPF.Common"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Width="800"
	Height="450"
	BorderThickness="0"
	SystemBackdropType="TransientWindow"
	mc:Ignorable="d"
	>
	<c:BackdropWindow.Resources>
		<m:DebuggerConverter x:Key="Debugger" />
	</c:BackdropWindow.Resources>

	<TabControl
		Padding="0"
		Background="Transparent"
		BorderThickness="0"
		>
		<TabItem Header="Tests">
			<StackPanel>
				<TextBox Width="276" />
				<TextBox Width="276" IsEnabled="False" Text="Hello world!" />

				<RadioButton GroupName="SystemBackdropType" Checked="SystemBackdropTypeChange">Mica</RadioButton>
				<RadioButton GroupName="SystemBackdropType" Checked="SystemBackdropTypeChange">MicaAlt</RadioButton>
				<RadioButton GroupName="SystemBackdropType" Checked="SystemBackdropTypeChange" IsChecked="True">Acrylic</RadioButton>
				<RadioButton GroupName="SystemBackdropType" Checked="SystemBackdropTypeChange">Solid</RadioButton>
				<RadioButton IsEnabled="False">Disabled Unchecked</RadioButton>
				<RadioButton IsChecked="True" IsEnabled="False">Disabled Checked</RadioButton>

				<TextBox x:Name="X" Text="0" />
				<Border Width="100" Height="100" BorderBrush="Gray" BorderThickness="1">
					<Canvas>
						<c:ColorTrackThumb Foreground="Lime" X="{Binding Text, ElementName=X, Mode=OneWay}" Y="0" />
					</Canvas>
				</Border>
			</StackPanel>
		</TabItem>
		<TabItem Header="Scroll Bar">
			<ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible">
				<Canvas Width="8192" Height="4096" />
			</ScrollViewer>
		</TabItem>
	</TabControl>
</c:BackdropWindow>
