@en # FAQ
@zh # 常见问题解答

@en > This Index was created in mind to help guide users on what features do what and how to use them.
@zh > 创建此索引的目的是帮助指导用户了解哪些功能、做什么以及如何使用它们。

::: important
@en If you found a bug, [create an issue](https://github.com/otomad/OtomadHelper/issues) on the GitHub page.
@zh 如果你发现了一个错误，请在GitHub页面上[创建一条议题](https://github.com/otomad/OtomadHelper/issues)。
@en Check the **FAQ BEFORE** you make an issue.
@zh 在提出议题之前，请**务必先**查看**常见问题解答**。

@en _This script is intended only to assist in the creation of YTPMVs and otoMADs._
@zh _此脚本仅用于辅助创建音MAD和YTPMV。_
@en _It is not designed to fully automate your workflow. Please do not rely on it to do all the editing work for you._
@zh _它不是为了完全自动化你的工作流程而设计的，请不要依赖它为你做所有的编辑工作。_
:::

## Basic

::: details ### What do these “specific” features do? / Where is “feature”.
[The documentation](./introduction.md) elaborates and indicates what each feature does and their respective functions. You should read there if you’re confused about what features do what and how they work.
:::

::: details ### My MIDI File isn’t working.
Make sure you have exported the MIDI file with a compatible program (e.g. FL Studio) and it has MIDI Channels/Tracks. A MIDI file that has no channels or tracks in it will not load or not correctly work with the script. If the MIDI is the MIDI file itself, try re-exporting the MIDI by re-importing it into the same program it was exported from and exporting it again.

If the MIDI file is from FL Studio. Make sure you go to [[Tools > Macros > Prepare for MIDI Export]], then Export as a .mid file.

If the MIDI file is from Reaper. When you’re done with the project, go to Export Project MIDI, select your input tracks to Entire Project and All Tracks. Set the output to Multitrack MIDI file (type 1) and hit OK

If it still doesn’t work, it may likely be [Velocity Error!](#velocity-error) or [Could not load file or assembly “NAudio”](#could-not-load-file-or-assembly-naudio-operation-is-not-supported).
:::

::: details ### I’m seeing white shapes and not the visual. / Can’t create drums.
This is being generated by the Sonar feature. Sonar will automatically turn on if your MIDI detects or has a drum track/channel and is selected. If you do not wish to have these types of visuals, promptly disable the feature in your configuation.
:::

::: details ### Otomad Helper doesn’t work when I run it.
Make sure you have the latest corresponding version for the Vegas Pro version you have. 13, 14 – 15, and 16 – 2026 each have their own different version. [Download the version](https://github.com/otomad/OtomadHelper/releases/latest) that matches your Vegas Pro version.

You also need NAudio installed alongside it, which comes with the script, this is required. Make sure NAudio has its respective folder in the script menu folder.

Always grab the script from [Releases](https://github.com/otomad/OtomadHelper/releases/latest), not the source code!
:::

::: details ### I downloaded both .cs and .dll releases, is that ok?
The script now comes in 2 packages. The .CS package and the .DLL package. The DLL version will run faster than .CS.

**You can NOT have both at the same time.** If you use the DLL version of the script, you must remove the .cs and .cs.config versions and vice versa!
:::

::: details ### Could not load file or assembly “NAudio” / Operation is not supported
Make sure you have unblocked the NAudio DLLs. Go to the DLL Folder, right-click the DLL, go to properties. if you see it says it’s blocked, unblock it.
:::

::: details ### Velocity Error!
```
System.DivideByZeroException: Attempted to divide by zero.
```

This is caused by the input values of the mapping velocity feature to be set the same. Make sure the value inputs are not the same.

```
System.FormatException: Got an off without an on 0 NoteOn
```

Your MIDI file has a note that has a velocity of 0. Check to make sure there isn’t any notes that have a velocity value of 0 (aka notes that have no volume)

**Example:**

![Note Off Example](/img/v4/faq/note_off_example.png)

[How to find the offending note?](#got-an-off-without-an-on-0-noteon-how-to-find-the-offending-note)
:::

:::: details ### ~~Specified argument was out of the range of valid values.~~
::: important
*This is fixed in Version 4.64.17.0, so it should not be reproduced in the latest version.*
:::

```
at ScriptPortal.Vegas.Fade.set_Gain(Single value)
```

This is caused by Mapping Velocity being turned on and having a value set that is higher or lower than what is used in the MIDI file. Change the Mapping Velocity back to 0 ~ 127 and Volume to 0 ~ 100 or disable Mapping Velocity. (Most Likely Scenario)

[How does Mapping Velocity Working?](#mapping-velocity-explanation)
::::

## Installation Related

::: details ### new 表达式要求在类型后有 () 或 []
**错误类型：**Vegas版本过旧，例如Vegas 12出现的问题

**报错时机：**在菜单运行脚本时

**错误信息：**
```
C:\Program Files\Sony\Vegas Pro 12.0\Script Menu\otomad_helper.cs(17): new 表达式要求在类型后有 () 或 []
```

**解决方法：**使用新版本的Vegas

**补充说明：**我之前错误宣称脚本可以在12至15的任意版本使用，实际上12很可能是无法使用的，如果造成不便我非常抱歉。由于我自己没有Vegas 12，所以不清楚确切的原因，推断是因为旧版Vegas使用了不同的.NET框架，所以不支持某些语法。
:::

::: details ### 未能找到类型或命名空间名称“Sony”
**错误类型：**Vegas版本不符，版本不符的报错

**报错时机：**在菜单运行脚本时

**错误信息：**
```
C:\Program Files\VEGAS\VEGAS Pro 15.0\Script Menu\otomad_helper.cs(11)：未能找到类型或命名空间名称“Sony”(是否缺少 using 指令或程序集引用?)
C:\Program Files\VEGAS\VEGAS Pro 15.0\Script Menu\otomad_helper.cs(657)：未能找到类型或命名空间名称"Vegas”(是否缺少 using 指令或程序集引用?)
```

**解决方法：**使用与Vegas版本对应的脚本

**补充说明：**Vegas 13与Vegas 14/15所对应的脚本是有一些不同的，我在Github的release里分别放出了两个下载链接，请使用正确的版本。具体来说，Vegas系列软件自14起被索尼出售给了MAGIX，所以一些库的命名也变化了，不再含有“Sony”字样。
:::

::: details ### 未能找到元数据文件“NAudio.dll”
**错误类型：**找不到NAudio.dll，找不到DLL的报错

**报错时机：**在菜单运行脚本时

**错误信息：**
```
C:\Program Files\VEGAS\VEGAS Pro 15.0\Script Menu\otomad_helper.cs:未能找到元数据文件“C:\Program Files\VEGAS\VEGAS Pro 15.0\Script Menu\DLL\NAudio.dll”
```

**解决方法：**确保将NAudio.dll放置到正确的位置；确保此文件已被解除保护。

**补充说明：**请参阅[安装](./installation.md)方法的说明。[NAudio](https://github.com/naudio/NAudio)是一个用于处理音频文件的库，我调用它来读取MIDI文件，因此NAudio.dll这个文件是必不可少的。
:::

:::: details ### 无法读取MIDI文件
**错误类型：**无法读取MIDI文件，读取MIDI失败的报错

**报错时机：**读取MIDI文件后 或 生成音频\视频途中

**错误信息：**
::: code-group
```[报错1]
System.Reflection.TargetInvocationException:调用的目标发生了异常。 ---> System.FormatException: Not a MIDI file - header chunk missing
  在 NAudio.Midi.MidiFile ..ctor(Stream inputStream, Boolean strictChecking, Boolean ownInputStream)
  在 EntryPoint.FromVegas(Vegas vegas)
  --- 内部异常堆栈跟踪的结尾 ---
  在 System.RuntimeMethodHandle.InvokeMethod(Object target, Object[] arguments, Signature sig, Boolean constructor)
  在 System.Reflection.RuntimeMethodInfo.UnsafeInvokeInternal(Object obj, Object[] parameters, Object[] arguments)
  在 System.Reflection.RuntimeMethodInfo.Invoke(Object obj, BindingFlags invokeAttr, Binder binder, Object[] parameters, CultureInfo culture)
  在 ScriptPortal.Vegas.ScriptHost.ScriptManager.Run(Assembly asm, String className, String methodName)
  在 ScriptPortal.Vegas.ScriptHost.RunScript(Boolean fCompileOnly)
```
```[报错2]
System.Reflection. TargetInvocationException: Exception has been thrown by the target of an invocation. ---> System.NullReferenceException: Object reference not set to an instance of an object.
  at EntryPoint.FromVegas(Vegas vegas)
  --- End of inner exception stack trace ---
  at System.RuntimeMethodHandle.InvokeMethod(Object target, Object[] arguments, Signature sig, Boolean constructor)
  at System.Reflection.RuntimeMethodInfo.UnsafeInvokeInternal(Object obj, Object[] parameters, Object[] arguments)
  at System.Reflection.RuntimeMethodInfo.Invoke(Object obj, BindingFlags invokeAttr, Binder binder, Object[] parameters, CultureInfo culture)
  at Sony.Vegas.ScriptHost.ScriptManager.Run(Assembly asm, String className, String methodName)
  at Sony.Vegas.ScriptHost.RunScript(Boolean fCompileOnly)
```
:::

**解决方法：**用宿主软件导入该MIDI，然后重新输出一个新的MIDI文件。

**补充说明：**MIDI文件有多种格式，脚本不保证都能够正确读取。所幸主流宿主软件在默认设置下导出的MIDI文件一般是可以读取的。（目前测试过FL Studio、LMMS与Music Studio for iPad）
::::

:::: details ### ~~无法调用移调插件~~
::: important
*该错误已过时，仅在Otomad Helper v0.1中出现，最新版不应发生该错误。*
:::

**错误类型：**无法调用移调插件，找不到调音插件的报错

**报错时机：**生成音频/视频途中

**错误信息：**
```
System.Reflection.TargetInvocationException:调用的目标发生了异常。 ---> System.ArgumentNullException:值不能为 null。
  在 ScriptPortal.Vegas.Effect..ctor(PlugInNode plugIn)
  在 EntryPoint.FromVegas(Vegas vegas)
  --- 内部异常堆栈跟踪的结尾 ---
  在 System.RuntimeMethodHandle.InvokeMethod(Object target, Object[] arguments, Signature sig, Boolean constructor)
  在 System.Reflection.RuntimeMethodInfo.UnsafeInvokeInternal(Object obj, Object[] parameters, Object[] arguments)
  在 System.Reflection.RuntimeMethodInfo.Invoke(Object obj, BindingFlags invokeAttr, Binder binder, Object[] parameters, CultureInfo culture)
  在 ScriptPortal.Vegas.ScriptHost.ScriptManager.Run(Assembly asm, String className, String methodName)
  在 ScriptPortal.Vegas.ScriptHost.RunScript(Boolean fCompileOnly)
```

**解决方法：**确保你的Vegas含有“移调”插件，并且名称已改为“Pitch Shift”。

**补充说明：**具体可见之前专栏中对于安装方法的说明。如果仍然不能解决，另一种方式是找到脚本中的所有的
```CSharp
vegas.AudioFX.FindChildByName("Pitch Shift");
```
字样（一共4处），并将它替换为
```CSharp
vegas.AudioFX.FindChildByUniqueID("{ED1B4100-93BE-11D0-AEBC-00A0C9053912}");
```
但这个办法也不保证能成功。这类错误留有一些疑点，我还没有完全解决。
::::

:::: details ### ~~无法调用移调插件的预设效果~~
::: important
*该错误已过时，仅在Otomad Helper v0.1中出现，最新版不应发生该错误。*
:::

**错误类型：**无法调用移调插件的预设效果，找不到预设效果的报错

**报错时机：**生成音频/视频途中

**错误信息：**
```
System.Reflection.TargetInvocationException:调用的目标发生了异常。 --->
System.Runtime.InteropServices.COMException:对COM组件的调用返回了错误HRESULT E_FAIL。
  在 ScriptPortal.Vegas.IEffectCOM.SetPreset(UInt32 sessionID, Int64 position, String presetName)
  在 EntryPoint.FromVegas(Vegas vegas)
  --- 内部异常堆栈跟踪的结尾 ---
  在 System.RuntimeMethodHandle.InvokeMethod(Object target, Object[] arguments, Signature sig, Boolean constructor)
  在 System.Reflection.RuntimeMethodInfo.UnsafeInvokeInternal(Object obj, Object[] parameters, Object[] arguments)
  在 System.Reflection.RuntimeMethodInfo.Invoke(Object obj, BindingFlags invokeAttr, Binder binder, Object[] parameters, CultureInfo culture)
  在 ScriptPortal.Vegas.ScriptHost.ScriptManager.Run(Assembly asm, String className, String methodName)
  在 ScriptPortal.Vegas.ScriptHost.RunScript(Boolean fCompileOnly)
```

**解决方法：**确保在移调插件中手动添加了所有的25个预设，且命名正确。

**补充说明：**具体可见我之前专栏中对于安装方法的说明。这25个预设是上下一个八度以内的所有变调种类，缺少任何一个都有可能出错。手动添加预设的确非常麻烦，但Vegas无法使用脚本来指定变调的具体参数，因此只好绕这个弯子。
::::

::: details ### 无法读取媒体流

**错误类型：**无法读取媒体流，无法读取媒体流的报错

**报错时机：**生成音频/视频途中

**错误信息：**
```
System.Reflection.TargetInvocationException:调用的目标发生了异常。 ---> System.ArgumentException: Media stream not specified.
  在 Sony.Vegas.Take.AddSelf(UInt32 trackID, Int64 eventID, MediaType mediaType)
  在 Sony.Vegas.TrackEvent.AddTake(MediaStream mediaStream)
  在 EntryPoint.FromVegas(Vegas vegas)
  --- 内部异常堆栈跟踪的结尾 ---
  在 System.RuntimeMethodHandle.InvokeMethod(Object target, Object[] arguments, Signature sig, Boolean constructor)
  在 System.Reflection.RuntimeMethodInfo.UnsafeInvokeInternal(Object obj, Object[] parameters, Object[] arguments)
  在 System.Reflection.RuntimeMethodInfo.Invoke(Object obj, BindingFlags invokeAttr, Binder binder, Object[] parameters, CultureInfo culture)
  在 Sony.Vegas.ScriptHost.ScriptManager.Run(Assembly asm, String className, String methodName)
  在 Sony.Vegas.ScriptHost.RunScript(Boolean fCompileOnly)
```

**解决方法：**在设置界面，纯音频素材不要勾选“包含视频”；纯视频素材不要勾选“包含音频”。

**补充说明：**若仍不能解决，说明该素材文件可能是Vegas不支持的格式，可以手动把该文件拖入Vegas中看一下是否视频音频都正常。
:::

## Deep Exploration

::: details ### Replace Track Events Separately Explanation {.italic-list}
1. Select a clip that you want to use as the replacement and set it as the replacement…
2. Click the top “Back to Vegas”…
3. Select the Clips you want to replace…
4. Run the Script Again…
5. Set the Replaced Selected…
6. Click Replace…

This will replace the clips with what you selected for the replacement.
:::

::: details ### Got an off without an on 0 NoteOn (How to find the offending note)
* Value of $\frac{\text{Offending Note}}{\text{Timebase}}$ (PPQ).
* Answer is $\text{Value}+1$ in `Bar:Beat:Tick` is where the off value note is.
* Or the beat it is on (e.g. $1536\div96=16=5:01:00$ in `Bar:Beat:Tick`).
:::

:::: details ### Mapping Velocity Explanation
* Mapping Velocity will generate events in the timeline where the opacity (for video) or volume (for audio) corresponds directly to the velocity value of each MIDI note, based on the range you set in the script.
* The default MIDI velocity range is 0 – 127.
  You can set a custom velocity range and map it to your desired opacity or volume range.
::: tip Example
If you set the velocity range to 64 – 127 and the opacity/volume range to 50% – 100%:
Notes with velocity 64 (Half velocity in FL Studio or Reaper) will generate at 50% opacity/volume. And notes with velocity 127 will generate at 100% opacity/volume.
:::
::: important
Mapping Velocity does not ignore notes outside your set range.
* If a note’s velocity is lower than your minimum setting, the generated event will be muted (0% volume / fully transparent).
* If a note’s velocity is higher than your maximum setting, the generated event will be set to 100% opacity/volume.
:::
::::

::: details ### Audio Bus Track Bugs caused by Vegas Pro
When using the audio bus track feature in certain versions of Vegas Pro on certain devices, rendering WAV *(Microsoft Wave)* and W64 *(Sony Wave64)* formats may be impossible, and all Audio FX parameters of audio bus tracks may be lost when rendering to other formats. If you accidentally encounter this bug, it is recommended that you either change your Vegas Pro version, or set it to unroute audio tracks and manually delete all audio bus tracks to avoid rendering issues.
:::
